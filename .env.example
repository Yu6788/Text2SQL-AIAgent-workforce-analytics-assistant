# Offline Demo does not require these values.
# For Live API mode, use an OpenAI-compatible provider.
LLM_PROVIDER=openai_compatible
LLM_MODEL=<model-id>
LLM_API_KEY=<your-api-key>
LLM_BASE_URL=<provider-base-url>
